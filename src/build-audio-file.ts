import { buildAudioFile } from "./audio/buildAudioFile";

const buildAudioFileScript = async () => {
//   const { steps, stepsAudioPath } = loadSteps();
//   await buildAudioFile(
//     stepsAudioPath,
//     steps.map((step) => `${stepsAudioPath}/${step.id}.mp3`),
//     steps.map((step) => step.audioStartTime)
//   );
};
